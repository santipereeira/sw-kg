En este caso creamos dos ficheros de shapes, el primero lo generamos a partir de la ontología y los mappings mendiante SCOOP-UI, y el segundo fichero lo generamos con este comando en la terminal **python -m shexer -i knowledge-graph.nt -o shapes_from_data.ttl**, que genera a partir del RDF, lo que nos va a servir postereiormente para comparar en pyshacl. Para generar este segundo archivo, tuvimos problemas ya que se nos generaban lineas repetidas por culpa windows que paralizaba el proceso, entonces tuvimos que rehacer el cofign.ini y ponerle como variable el numero de procesos a 1, para solucionar el problema, y generar de nuevo todo, y con esto ya nos funcionó. 
Para la validación, le pedimos a chatGPT que nos generara los .py para poder hacer la validación con pyshacl, a partir de estos scripts pudimos generar los resultados de validación correpondientes (report_data_shapes y report_model_shapes). En este punto también nos encontramos con una serie de problemas y es que el .nt (RDF) tenia codificados los sitios web, con espacios y caracteres raros, lo que nos impedia generar bien estos archivos, entonces le comentamos este problema a chat y adapto el programa (.py) para que detectara estos errores y los arreglara directamente sobre el RDF entonces digamos que fue una solución rápida pero que no se debería utilizar a largo plazo porque es un error que arrastramos en el resto del proyecto.